import reducer from './reducer';
import { setError, setLoading, setSuccess } from './actions';
import initialHookState from './initialHookState';

export default reducer;

export {
  setError,
  setSuccess,
  setLoading,
  initialHookState,
};
